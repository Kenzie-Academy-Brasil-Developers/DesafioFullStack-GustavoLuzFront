import { DefaultTemplate } from "../../components/DefaultTemplate";

export const DashBoard = () => {
   return (
      <DefaultTemplate>
         <main>
            <div className="container">

            </div>
         </main>
      </DefaultTemplate>

   );
};